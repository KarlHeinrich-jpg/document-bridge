# 安全说明

文桥默认只监听 `127.0.0.1`，不应直接暴露到公共互联网。上传文件会在系统临时目录中处理，并在响应结束后清理。

项目 ZIP 受到以下限制：

- 网页上传最大 50 MB；
- 最多 1,000 个压缩条目；
- 解压后总体积最大 100 MB；
- 拒绝绝对路径、`..` 路径穿越与符号链接；
- 单次转换最长运行 120 秒。

如果发现安全问题，请不要在公开 Issue 中附带敏感文档。可通过 GitHub 仓库的 Security 页面私下报告。

